# ============================================================================
# sap-toolkit — Python dependencies (aXet.code SAP + Office skills)
# Install once:  pip install -r requirements.txt
# (office-tools prefers an isolated venv — see README.)
# ============================================================================

# --- SAP ADT read-only server (sap-consultant/sap-adt-readonly) ---------------------
requests>=2.25.0
# Security: pin transitive idna floor — CVE-2026-45409 (DoS via idna.encode on
# crafted Unicode) is fixed in 3.15. requests pulls idna in transitively.
idna>=3.15
python-dotenv>=0.19.0
# The read-only HTTP gate imports the vendored ADT engine, which imports FastMCP.
mcp>=1.0.0

# --- RFC bridge (sap-consultant/sap-adt-readonly, ONLY for systems behind a router
# that denies raw/native TCP to the ADT/ICM port — see SKILL.md "Router-only
# sistemler (RFC bridge)") -----------------------------------------------
# When used through aXet SAP Launcher, pyrfc + the SAP NW RFC SDK are already
# bundled in the app (resources/rfc-runtime) — nothing to install. This entry
# is only for running adt_rfc_probe.py/adt_rfc_bridge.py standalone, outside
# the launcher, with your own Python:
#   1. Download "SAP NW RFC SDK" from https://support.sap.com/en/product/connectors/nwrfcsdk.html
#      (requires an S-user with download authorization for this object).
#   2. Extract it, set SAPNWRFC_HOME to the extracted folder.
#   3. py -m pip install pyrfc
# adt_rfc_probe.py / adt_rfc_bridge.py give a clear error if this isn't done.
# pyrfc>=3.3

# --- office-tools -----------------------------------------------------------
# Excel skills
pandas>=2.0.0
polars>=0.20.0
openpyxl>=3.1.0
xlrd>=2.0.1

# Document generators
markdown>=3.5         # office-pdf: Markdown -> HTML
python-pptx>=0.6.23   # office-pptx: native editable .pptx
python-docx>=1.1.0    # office-docx: Word .docx

# NOTE: office-pdf's rendering engine (headless Chromium) is installed ON DEMAND,
# not here (~130 MB):
#     py -m pip install playwright && py -m playwright install chromium
# The office-pdf script prints this command if Playwright is missing.

# abapgit-bridge uses the Python standard library only — no dependencies.

# --- ts-generator / fs-generator (sap-consultant/skills/{ts-generator,fs-generator}) ---
# Python: stdlib only. They render PDFs by shelling out to office-pdf's
# md_to_pdf.py, so they reuse the markdown + playwright deps above.
# Node.js: PDF text extraction needs pdfjs-dist — installed separately via
#     cd sap-consultant/skills/ts-generator/scripts && npm install
#     cd sap-consultant/skills/fs-generator/scripts && npm install


# --- axet-flows (axet-flows/skills/axet-flows) -------------------------------
# Python: yalnizca standart kutuphane (json, urllib, ssl, argparse). Kurulacak
# bir bagimlilik YOK -- scriptler akis JSON'u okuyup dogruluyor ve tasarimcinin
# admin API'sine konusuyor, o kadar.

# --- ntt-s4-migrator / ntt-atc-batch-remediator ------------------------------
# conversion-scope (donusum kapsami) ve atc-remediation (toplu ATC duzeltme)
# SAP'a HIC baglanmiyor -- danismanin disa aktardigi dosyalari okuyorlar. Ama o
# dosyalar Excel/PDF/HTML/XML olabildigi icin ayristirici bagimliliklari var:
pydantic>=2.0.0
click>=8.0.0
PyYAML>=6.0
lxml>=5.0.0
beautifulsoup4>=4.12.0
networkx>=3.0
pdfplumber>=0.11.0

# --- sapgui-screenshots (sapgui-scriptter) ----------------------------------
# SAP GUI penceresini PrintWindow ile yakaliyor: Windows API + goruntu kaydi.
# aXet SAP Launcher icinden kullanildiginda bunlar uygulamanin kendi
# calisma zamaninda (resources/guiscript-runtime) zaten var; asagidaki satirlar
# script'i kendi Python'unla, uygulama disinda calistirmak icin:
pywin32>=306 ; sys_platform == "win32"
Pillow>=10.0.0

# --- sap-enduser-doc (sap-consultant/skills/sap-enduser-doc) -----------------
# Python degil NODE bagimliligi: playwright + marked + sharp + mermaid.
# Pakette GELMIYOR ve otomatik kurulmuyor -- tek seferlik, kullanici onayli:
#     cd .axet-code/skills/sap-enduser-doc && npm install
# Once `node scripts/precheck.js` calistir; eksikse dokumana hic baslama.

# --- celonis-ocpm-builder (celonis) -----------------------------------------
# Python: stdlib + requests (yukarida). Celonis REST API'sine konusuyor;
# SAP'a degil. Kimlik bilgisi kullanicinin kendi Celonis ortamindan gelir.

# --- sap-datasphere / sap-ecosystem paketleri -------------------------------
# Yalnizca SKILL.md: yukari akistaki paketlere yonlendiren rehber dosyalari.
# Calistirilacak script yok, bagimlilik yok.
