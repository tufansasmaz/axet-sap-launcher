# Excel skills
pandas>=2.0.0
polars>=0.20.0
openpyxl>=3.1.0
xlrd>=2.0.1

# Document generators
markdown>=3.5      # office-pdf: Markdown -> HTML
python-pptx>=0.6.23  # office-pptx: native editable .pptx
python-docx>=1.1.0   # office-docx: Word .docx

# office-pdf rendering engine (headless Chromium) is installed ON DEMAND, not here:
#   py -m pip install playwright   &&   py -m playwright install chromium
# ~130 MB one-time. The office-pdf script prints this command if Playwright is missing.
